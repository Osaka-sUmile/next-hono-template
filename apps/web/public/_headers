# Cloudflare Workers Assets (Workers Static Assets) は next.config.mjs の headers() を
# 経由しないため、静的アセットへの長期キャッシュ設定はここで補う。
# see: https://developers.cloudflare.com/workers/static-assets/headers/
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
